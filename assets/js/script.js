// Query selectors
var btn = window.document.querySelector(".btn");
// console.dir(btn);

// Global Variable Declarations
const button = document.querySelector('button');
let timerel = document.getElementById("timer");




// Upon start button (Quiz Init)

// Time Counter is initiated



// Question(s) are presented 

// If answer is correct, "Correct" indication is presented 

// Score is added 

// User is moved to next question

// If incorrect, "Wrong" indication is presented

// Time is subtracted from Time Counter

// User is moved to next question

// Game is over when all questions are answered

// Game is over when Time Counter === 0

// User can input initials and game score is saved

button.addEventListener('click', event => {
   
  });